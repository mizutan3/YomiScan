services:
  - type: web
    name: ocr-server
    runtime: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn server:app"
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.2"
    env: python
    plan: free
