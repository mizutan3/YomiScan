services:
  - type: web
    name: ocr-server
    runtime: python
    buildCommand: |
      chmod +x build.sh
      ./build.sh
    startCommand: "gunicorn server:app"
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.2"
    env: python
    plan: free
